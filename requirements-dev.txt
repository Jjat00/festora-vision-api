# Development and testing dependencies — not installed in production.
# Versions verified against PyPI latest as of 2026-02-17.
-r requirements.txt

pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
httpx==0.28.1          # AsyncClient for FastAPI TestClient
respx==0.22.0          # Mock httpx requests in tests
ruff==0.15.1           # Linting + formatting
mypy==1.19.1
types-Pillow==10.2.0.20240822
